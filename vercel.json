{
  "version": 2,
  "buildCommand": "npm install @tailwindcss/oxide-linux-x64-gnu @tailwindcss/oxide-linux-x64-musl && npx prisma db-generate && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}